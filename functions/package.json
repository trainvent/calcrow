{
  "name": "calcrow-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^6.5.0",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/nodemailer": "^6.4.17",
    "typescript": "^5.9.2"
  }
}
